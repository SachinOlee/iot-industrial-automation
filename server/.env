# // server/.env (Merged configuration with MongoDB Atlas priority)
NODE_ENV=development
PORT=5000

# Database Configuration
# MongoDB Atlas Connection (Primary) - Update with your credentials
# MONGODB_URI=mongodb+srv://yourusername:yourpassword@cluster0.xxxxx.mongodb.net/iot_automation?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/iot_automation

# Local MongoDB Connection (Fallback - uncomment if using local MongoDB)
# MONGODB_URI=mongodb://localhost:27017/iot_automation

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random_123456789
JWT_EXPIRE=7d
JWT_COOKIE_EXPIRE=7

# Email Configuration (REQUIRED for user registration)
# For Gmail: Use your Gmail address and App Password (not regular password)
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password: https://support.google.com/accounts/answer/185833
# 3. Use the App Password below (not your regular Gmail password)

EMAIL_FROM=noreply@yourcompany.com
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Legacy SMTP Configuration (for backward compatibility)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD=your_app_password
FROM_NAME=IoT Industrial Automation

# Frontend URL
CLIENT_URL=http://localhost:3000

# ML Service URL
ML_SERVICE_URL=http://localhost:8000

# Development Options
# Set to 'true' to skip email verification (for development/testing only)
SKIP_EMAIL_VERIFICATION=false